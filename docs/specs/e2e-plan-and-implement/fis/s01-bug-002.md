# Fix BUG-002 — persist edited due dates

## Feature Overview and Goal

**Intent**: Close BUG-002 — a due date set in the edit dialog is lost after save. The update handler must read the submitted due-date field and persist it on the todo, and the edit dialog must pre-fill the current value.

## Acceptance Scenarios

- Editing a todo, setting a due date, and saving persists the due date so it is still present after the list re-renders.
- The edit dialog pre-populates the existing due date when reopened.

## Implementation Plan

- In src/app/routes/todos.py, read the due-date form field in the update handler and store it on the todo.
- In src/app/templates/app.html, bind the edit dialog's due-date input to the todo's current value.

## Implementation Observations

### Run: 2026-08-13

#### NOTICED BUT NOT TOUCHING

- `tests/test_todos.py::TestTodos::test_create_todo` fails because quick-added todos have no default priority; this is BUG-003 and outside S01 scope.

#### IMPLEMENTATION

- The edit dialog already binds the current value through the todo partial's `data-todo-due-date` attribute and `openEditTodoDialog`; S01 regression coverage confirms the rendered value is `YYYY-MM-DD`.

### Run: 2026-08-13 19:14 UTC – observations

#### IMPLEMENTATION

- Added `tests/test_todos.py::TestTodos::test_edit_dialog_assigns_due_date_to_input`, which executes `app.js` with a minimal Node VM DOM stub and asserts the date input receives `2025-12-31`; verification: targeted 2 passed, full `uv run pytest -q` 60 passed, `compileall` and `git diff --check` passed.
